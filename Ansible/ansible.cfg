[defaults]
inventory=../Local/hosts-real
host_key_checking = True
nocolor = True
retry_files_enabled = False
pipelining = True
forks = 40
interpreter_python = auto_silent

private_key_file = ~/.ssh/id_ed25519_ansible-on-dev
remote_user = ansible
allow_world_readable_tmpfiles = True
